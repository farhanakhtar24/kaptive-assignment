import React from "react";
import styles from "./page.module.css";

type Props = {};

const page = (props: Props) => {
	return <div className={styles["page-container"]}>page</div>;
};

export default page;
